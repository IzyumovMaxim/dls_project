name: bge_large_flat

model:
  name: BAAI/bge-large-en-v1.5
  normalize: true
  query_prompt: query
  doc_prompt: document
  batch_size: 32
  device: cuda

index:
  type: flat

eval:
  top_k: 100
  k_values: [1, 5, 10, 100]