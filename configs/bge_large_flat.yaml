name: bge_large_flat

model:
  name: BAAI/bge-large-en-v1.5
  normalize: true
  query_prompt: null
  doc_prompt: null
  batch_size: 128
  fp16: true
  device: cuda

index:
  type: flat

eval:
  top_k: 100
  k_values: [1, 5, 10, 100]
