name: bge_small_flat

model:
  name: BAAI/bge-small-en-v1.5
  normalize: true
  query_prompt: query
  doc_prompt: document
  batch_size: 64

index:
  type: flat

eval:
  top_k: 100
  k_values: [1, 5, 10, 100]
