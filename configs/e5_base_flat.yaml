name: e5_base_flat

model:
  name: intfloat/e5-base-v2
  normalize: true
  query_prompt: "query: "      # e5 is trained with query:/passage: prefixes on BOTH sides
  doc_prompt: "passage: "
  batch_size: 256
  device: cuda

index:
  type: flat

eval:
  top_k: 100
  k_values: [1, 5, 10, 100]